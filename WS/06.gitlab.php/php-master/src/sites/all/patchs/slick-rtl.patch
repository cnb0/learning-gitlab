# This patch file was generated by NetBeans IDE
# It uses platform neutral UTF-8 encoding and \n newlines.
--- <html>slick.module (<b>f947d84</b>)</html>
+++ <html><b>Current File</b></html>
@@ -365,6 +365,16 @@
   $id       = $settings['attributes']['id'] = empty($id) ? $fallback : $id;
   $display  = empty($settings['current_display']) ? 'main' : $settings['current_display'];
 
+  global $language;
+  if ($language->language == 'he') {
+      $rtl = TRUE;
+  }
+  else {
+      $rtl = FALSE;
+  }
+    
+  $optionset->options['settings']['rtl'] = $rtl;
+  
   $slick = array(
     '#theme'     => 'slick',
     '#items'     => $items,

